{% block extends %}
{{ "{% extends 'base.html.twig' %}" }}
{% endblock extends %}

{{ "{% block page_title %}" }}
    {{ "{% trans %}" }}New {{ entity_singularized|capitalize }}{{ "{% endtrans %}" }}
{{ "{% endblock %}" }}

{{ "{% block page_subtitle %}" }}
    {{ "{% trans %}" }}Please complete the following form{{ "{% endtrans %}" }}
{{ "{% endblock %}" }}

{% block body %}
{{ "{% block body %}" }}
	<div class="box box-primary">
        <div class="box-header with-border">
          <h3 class="box-title">{{ "{% trans %}" }}Form{{ "{% endtrans %}" }}</h3>
        </div>
		{{ '{{ form_start(form) }}' }}
			<div class="box-body">
		        {{ '{{ form_widget(form) }}' }}
	        </div>
	        <div class="box-footer">
	            <button type="submit" class="btn btn-primary">{{ "{% trans %}" }}Submit{{ "{% endtrans %}" }}</button>
	        </div>
	    {{ '{{ form_end(form) }}' }}
	</div>
    

{% set hide_edit, hide_delete = true, true %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}

