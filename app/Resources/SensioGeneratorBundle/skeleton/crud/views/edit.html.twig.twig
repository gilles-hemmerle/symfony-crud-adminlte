{% block extends %}
{{ "{% extends 'base.html.twig' %}" }}
{% endblock extends %}

{{ "{% block page_title %}" }}
    {{ "{% trans %}" }}{{ entity_singularized|capitalize }} edit{{ "{% endtrans %}" }}
{{ "{% endblock %}" }}

{{ "{% block page_subtitle %}" }}
    {{ "{% trans %}" }}Please complete the following form{{ "{% endtrans %}" }}
{{ "{% endblock %}" }}

{% block body %}
{{ "{% block body %}" }}
	<div class="box box-primary">
        <div class="box-header with-border">
          <h3 class="box-title">{{ "{% trans %}" }}Form{{ "{% endtrans %}" }}</h3>
        </div>
		{{ '{{ form_start(edit_form) }}' }}
			<div class="box-body">
		        {{ '{{ form_widget(edit_form) }}' }}
	        </div>
	        <div class="box-footer">
	            <button type="submit" class="btn btn-primary">{{ "{% trans %}" }}Submit{{ "{% endtrans %}" }}</button>
	        </div>
	    {{ '{{ form_end(edit_form) }}' }}
	</div>

    

{% set hide_edit, hide_delete = true, false %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}
